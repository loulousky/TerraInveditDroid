<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="MarcusD.TerraInvedit"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk
        android:minSdkVersion="11" android:targetSdkVersion="14"/>

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_label"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ActivityPlayers"
            android:label="@string/title_activity_activity_players" >
        </activity>
        <activity
            android:name=".InveditActivity"
            android:label="@string/title_activity_invedit" >
        </activity>
        <activity android:name=".ActivityItemedit" android:label="@string/title_activity_itemedit"></activity>
    </application>

</manifest>
